*** Settings ***
Test Setup        Run Keywords    Get Test Case ID From Test Case Name    AND    fsweb_Load Data From File    ${v_AddRecipientsDataFile}    ${v_testcaseID}    AND
...               Open Web App    AND    SignIn    ${v_data['Owner email address']}    ${v_data['Owner password']}
Test Teardown     Run Keywords    fsweb_Submit Test Status Into Testlink    ${v_testcaseID}    AND    Close All Browsers
Resource          ../Resource/AddRecipients.txt

*** Test Cases ***
fs[7176]_Can string a file to a FileString user
    No Operation

fs[7178]_Can string file to one non - FileString user
    [Tags]    RequiredMail
    No Operation

fs[7181]_Can string a file to secondary email of another FileString user
    No Operation

fs[7185]_Can string a file to un-verified secondary email before it is verified as secondary email of one user
    [Tags]    RequiredMail
    No Operation

fs[7187]_Can string a file to un-verified primary email before it really becomes primary email
    [Tags]    RequiredMail
    No Operation

fs[7190]_Can string a file to multi valid recipients at the same time
    No Operation

fs[7193]_ONLY send file to valid emails and show alert in case string a file to multi valid and invalid recipients at the same time
    No Operation
