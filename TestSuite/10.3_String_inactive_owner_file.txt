*** Settings ***
Test Setup        Run Keywords    Get Test Case ID From Test Case Name    AND    fsweb_Load Data From File    ${v_StringInactiveOwnerFileDataSet}    ${v_testcaseID}
Test Teardown     Run Keywords    fsweb_Submit Test Status Into Testlink    ${v_testcaseID}    AND    Close All Browsers
Resource          ../Resource/AddRecipients.txt

*** Test Cases ***
fs[17575]_Can string to new user
    Comment    fsweb_Sign Up Successful    automation team    test    ${v_data['Recipient account email']}    ${v_data['Recipient password']}    ${v_data['Recipient email address']}
    ...    ${v_data['Recipient email password']}
    #    Login and String file to new user
    Comment    Open Web App
    Comment    SignIn    ${v_data['Owner email address']}    ${v_data['Owner password']}
    Comment    Right Click File    ${v_data['Owner filename']}
    Comment    Click    ${MenuWhenRightClickOnAFile_String}
    Comment    fsweb_Input Recipient On Add Recipient Popup    ${v_data['Recipient email address']}
    Comment    Click    ${Reshare_StringButton}
    Comment    Sleep    5s
    Comment    close browser
    #    Verify email of recipient first
    Comment    fsweb_Verify Recipient Email When String Successfully    ${v_data['Recipient account email']}    ${v_data['Recipient password']}    ${v_data['Expected Email Subject']}    ${v_data['Expected Email Body']}
    #    Open new browser to verify file is
    Comment    Open Web App
    Comment    SignIn    ${v_data['Recipient account email']}    ${v_data['Recipient password']}
    Comment    fsweb_Open A File Or Folder
    Comment    fsweb_Open A File Or Folder    ${v_data['Owner fullname']}
    Comment    fsweb_Open A File Or Folder
    Comment    fsweb_Verify File Is Opened Correctly
    Log    1

fs[17577]_Can string to new non-user
    No Operation

fs[17579]_Can string to user that owner revoked him
    No Operation

fs[17581]_Can string to non-user that owner revoked him
    No Operation

fs[17583]_Can string to user that direct recipient revoked him
    No Operation

fs[17585]_Can string to non-user that direct recipient revoked him
    No Operation

fs[17587]_Can string to user has expired
    No Operation

fs[17589]_Can string to non-user has expired
    No Operation
